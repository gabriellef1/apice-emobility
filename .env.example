# Base path do site. "/" em dev e no domínio próprio; "/<nome-do-repo>/" no GitHub Pages.
# O workflow de deploy injeta este valor a partir do nome do repositório.
VITE_BASE_PATH=/

# URL pública do site (canonical, sitemap, OG). Sem barra no final.
VITE_SITE_URL=http://localhost:5173
